#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{

        int row, column, extra;
        cin >> row >> column >> extra;

        cout << (row + extra)* column << endl;
        return 0;
    }
    
