#include<iostream>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int length, width, cost;
        cin >> length >> width >> cost;
        cout << 2 * (length + width) * cost << endl;
    }
    return 0;
}
